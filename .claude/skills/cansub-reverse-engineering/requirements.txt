# cansub-reverse-engineering skill dependencies.
# Install into the repo's project-local venv via install.bat (Windows) or
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# The repo-root requirements.txt is a superset used by install.bat.
python-can>=4.6.0
python-can-cansub>=2026.6.2
cantools>=41.0
numpy>=2.0
pandas>=2.2
scipy>=1.13
matplotlib>=3.8
# phase 2 only:
flask>=3.0
# phase 3 (vision reference from a video) only:
rapidocr>=2
onnxruntime>=1.17
opencv-python>=4.9
# audio annotations (transcribe_audio) only; also needs ffmpeg on PATH:
faster-whisper>=1.1
